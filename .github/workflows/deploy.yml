# Deploy the book to GitHub Pages (python.ragworld.org).
# The custom domain lives in Settings -> Pages and is set by hand. The workflow
# token cannot change it: the Pages API asks the caller to be a repository
# administrator, and a CNAME file in the artifact is ignored for Actions-based
# publishing. Changing the domain therefore means editing that field and this file.
# Runs on pushes to main and when called from other workflows (the data auto-update).
name: deploy

on:
  push:
    branches: [main]
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Build the site
        run: |
          ./build.sh
          mkdir -p dist
          cp rag-python-advanced.html dist/index.html

      - name: Enable Pages if not enabled yet
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - id: deployment
        uses: actions/deploy-pages@v4
