# Extracted from the book automatically. Lines above the mark are not part of the listing.
from stubs import GraphTool, SearchTool, SqlTool  # noqa: F401
# --- the listing ---
TOOLS = {
    "search": SearchTool,
    "graph": GraphTool,
    "sql": SqlTool,
    # when adding a tool, remember to list it here
}
